#include <stdio.h>

int main() {

    //variable = A resuable container for a value.
    // Behaves as if iit were the value it contains
    int age = 20;  
    int year = 2026;
    int quantity = 1;

    printf("you are %d years old\n", age);
    printf("The half of the %d year\n", year);
    printf("you have ordered %d x items\n", quantity);

    return 0;
}