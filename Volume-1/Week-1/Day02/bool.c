#include <stdio.h>
#include <stdbool.h>

int main() {
    // variable = A resuable container for a value.
    // Behaves as if it were the value it contains

    bool isOnline = 1;

    printf("%d", isOnline);



    return 0;
}